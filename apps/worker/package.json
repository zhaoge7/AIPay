{
  "name": "@aipay/worker",
  "version": "0.0.0",
  "private": true,
  "description": "AIPay asynchronous worker",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "closed-test": "node --env-file-if-exists=../../.env scripts/start-closed-test.mjs",
    "test": "pnpm run build && node --test --test-concurrency=1 test/**/*.test.mjs"
  },
  "dependencies": {
    "@aipay/config": "workspace:*",
    "@aipay/contracts": "workspace:*",
    "@aipay/database": "workspace:*",
    "ipaddr.js": "2.5.0"
  }
}
